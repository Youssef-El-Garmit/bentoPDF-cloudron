server {
    listen 8080;
    server_name _;
    root /app/code;
    index index.html;

    types {
        text/javascript mjs;
        application/javascript js;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

